# Roadmap

Target for the next delivery cycle:

- connect the durable storage adapter;
- replay accepted canonical messages after a process restart;
- add crash and partial-write tests;
- publish adapter support only after runtime verification.

This file describes target behavior, not current delivery state.

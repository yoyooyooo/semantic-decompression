---
status: frozen
owns: definitions-and-invariants
---

# Semantic Root

A `Workflow` is the durable subject. A `RunAttempt` is one temporary execution of that workflow.

A `CandidateResult` becomes an accepted fact only after the workflow authority accepts it. UI notifications do not own durable truth.

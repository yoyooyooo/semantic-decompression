# Reports

The handoff says the latest local runtime evidence should be at `docs/reports/local-validation.md`.

That path is part of the review target. Do not assume the report exists without checking the package.

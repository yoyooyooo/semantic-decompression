# Product Direction

Status: target

The intended product will connect a real provider, persist accepted JobRecord values across restart, and expose projections in a browser interface.

These are product targets. Promotion requires implementation, integration evidence, restart evidence, and an accepted Claim.
